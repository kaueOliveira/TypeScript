//Muito usado quando queremos enumerar mais de uma opção de algo.

enum Cores {
    Vermelho, // 0
    Azul, // 1
    Amarelo, // 2
} 

enum Cores {
    Roxo = 12,
    Verde = 41,
    Rosa = 'Sakura',
}

console.log(Cores)